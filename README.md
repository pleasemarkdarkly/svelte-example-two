# Svelte, Typescript, Tailwind, Snowpack and Nutella 

This is a basic example repo experimenting with the confusing world of Svelte while they are working on SvelteKit.

```bash
npm install -g pnpm
pnpm install
pnpm run dev
```
